import { StyleSheet, Text, View } from 'react-native'
import React from 'react'

const ImageLazyLoad = () => {
  return (
    <View>
      <Text>ImageLazyLoad</Text>
    </View>
  )
}

export default ImageLazyLoad

const styles = StyleSheet.create({})